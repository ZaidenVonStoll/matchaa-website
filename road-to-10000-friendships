<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Matchaa — Road to 10,000 Friendships</title>
  <meta name="description" content="We're on a mission to help 10,000 people make one real friend. Track our progress and join the movement." />
  <link rel="icon" href="/favicon.png" />
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&family=Fraunces:ital,opsz,wght@0,9..144,300;0,9..144,400;0,9..144,500;0,9..144,600;1,9..144,300;1,9..144,400;1,9..144,500&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --green: #2d5e3a;
      --green-dark: #1a3d28;
      --sage: #5a8a6a;
      --sage-light: #eef5ea;
      --cream: #faf8f4;
      --white: #FBFFFE;
      --muted: #7a8c80;
      --border: #dce8d8;
      --text: #1a3d28;
      --lime: #97c459;
    }
    html { scroll-behavior: smooth; }
    body {
      background: var(--cream);
      color: var(--text);
      font-family: 'DM Sans', sans-serif;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* ── NAV ── */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      display: flex; align-items: center; justify-content: space-between;
      padding: 18px 40px;
      background: rgba(250,248,244,0.9); backdrop-filter: blur(14px);
      border-bottom: 1px solid var(--border);
    }
    .nav-logo { display: flex; align-items: center; gap: 10px; text-decoration: none; }
    .nav-mark {
      width: 36px; height: 36px; background: var(--green); border-radius: 10px;
      display: flex; align-items: center; justify-content: center;
    }
    .nav-mark img { width: 100%; height: 100%; object-fit: contain; padding: 5px; padding-bottom: 7px; }
    .nav-name { font-size: 18px; font-weight: 700; color: var(--text); letter-spacing: -0.3px; }
    .nav-links { display: flex; align-items: center; gap: 24px; }
    .nav-link { font-size: 14px; font-weight: 500; color: var(--sage); text-decoration: none; transition: color .2s; }
    .nav-link:hover { color: var(--green); }
    .nav-cta {
      background: var(--green); color: white; padding: 10px 22px;
      border-radius: 100px; font-size: 14px; font-weight: 600; text-decoration: none;
      transition: background .2s, transform .15s;
    }
    .nav-cta:hover { background: var(--green-dark); transform: translateY(-1px); }

    /* ── SECTIONS ── */
    section { padding: 0 24px; }
    .container { max-width: 800px; margin: 0 auto; }

    /* ── HERO ── */
    .hero {
      padding-top: 130px; padding-bottom: 40px;
      text-align: center;
    }
    .hero-eyebrow {
      font-size: 13px; font-weight: 600; letter-spacing: 0.06em;
      color: var(--sage); text-transform: uppercase;
      margin-bottom: 24px;
      animation: fadeUp .7s ease both;
    }
    .hero h1 {
      font-family: 'Fraunces', serif; font-weight: 500;
      font-size: clamp(36px, 7vw, 60px); line-height: 1.08;
      color: var(--text); margin-bottom: 20px;
      animation: fadeUp .7s .1s ease both;
    }
    .hero-sub {
      font-size: clamp(15px, 2.2vw, 17px); color: var(--sage);
      line-height: 1.7; max-width: 520px; margin: 0 auto 32px;
      animation: fadeUp .7s .2s ease both;
    }

    /* ── PROGRESS CARD (inside hero) ── */
    .progress-card {
      background: var(--white); border: 1px solid var(--border);
      border-radius: 24px; padding: clamp(28px, 4vw, 40px);
      box-shadow: 0 4px 24px rgba(0,0,0,.04);
      margin-bottom: 24px; text-align: left;
      animation: fadeUp .7s .3s ease both;
    }
    .progress-card-title {
      font-family: 'Fraunces', serif; font-size: 18px;
      font-weight: 500; color: var(--text); margin-bottom: 16px;
    }
    .progress-numbers {
      display: flex; align-items: baseline; gap: 12px;
      margin-bottom: 6px; flex-wrap: wrap;
    }
    .progress-current {
      font-family: 'Fraunces', serif; font-size: clamp(44px, 7vw, 64px);
      font-weight: 500; color: var(--text); line-height: 1;
    }
    .progress-unit {
      font-size: 16px; color: var(--sage); font-weight: 400;
    }
    .progress-meta {
      display: flex; gap: 20px; margin-bottom: 20px; flex-wrap: wrap;
    }
    .progress-meta-item {
      font-size: 13px; color: var(--muted); font-weight: 500;
    }
    .progress-track {
      width: 100%; height: 18px; background: var(--sage-light);
      border-radius: 100px; overflow: hidden; position: relative;
    }
    .progress-fill {
      height: 100%; background: linear-gradient(90deg, var(--green), var(--lime));
      border-radius: 100px; transition: width 1.5s cubic-bezier(.16,1,.3,1);
      min-width: 4px;
    }
    .progress-milestones {
      display: flex; justify-content: space-between;
      margin-top: 14px; padding: 0;
    }
    .milestone { text-align: center; flex: 1; }
    .milestone-val {
      font-size: 12px; font-weight: 700; color: var(--muted);
      display: block; margin-bottom: 2px;
    }
    .milestone-label {
      font-size: 10px; color: var(--muted); font-weight: 400;
      display: block; line-height: 1.3;
    }
    .milestone.reached .milestone-val { color: var(--green); }
    .milestone.reached .milestone-label { color: var(--sage); }
    .progress-footnote {
      text-align: center; margin-top: 18px;
      font-size: 13px; color: var(--muted); font-style: italic;
      font-family: 'Fraunces', serif;
    }

    /* ── JOINED + CTA row ── */
    .hero-bottom {
      display: flex; align-items: center; justify-content: space-between;
      flex-wrap: wrap; gap: 16px;
      animation: fadeUp .7s .4s ease both;
      margin-bottom: 8px;
    }
    .joined-stat {
      display: flex; align-items: baseline; gap: 8px;
    }
    .joined-num {
      font-family: 'Fraunces', serif; font-size: 28px;
      font-weight: 500; color: var(--text);
    }
    .joined-label { font-size: 14px; color: var(--sage); }
    .hero-cta {
      display: inline-block; background: var(--green); color: white;
      padding: 14px 32px; border-radius: 100px; font-size: 15px; font-weight: 600;
      text-decoration: none; box-shadow: 0 8px 32px rgba(45,94,58,.25);
      transition: all .25s;
    }
    .hero-cta:hover { background: var(--green-dark); transform: translateY(-2px); box-shadow: 0 12px 40px rgba(45,94,58,.35); }
    .hero-honest {
      text-align: center; font-size: 12px; color: var(--muted);
      margin-bottom: 0;
      animation: fadeUp .7s .45s ease both;
    }

    /* ── MISSION PULL-UP ── */
    .mission-pullup {
      padding-top: 40px; padding-bottom: 48px; text-align: center;
    }
    .mission-pullup h2 {
      font-family: 'Fraunces', serif; font-weight: 500;
      font-size: clamp(24px, 4vw, 32px); color: var(--text);
      margin-bottom: 12px; line-height: 1.2;
    }
    .mission-pullup p {
      font-size: clamp(14px, 2vw, 16px); color: var(--sage);
      line-height: 1.7; max-width: 480px; margin: 0 auto;
    }

    /* ── DIVIDER ── */
    .divider {
      width: 48px; height: 2px; background: var(--border);
      border-radius: 100px; margin: 0 auto;
    }

    /* ── HOW IT WORKS ── */
    .how-section { padding-top: 48px; padding-bottom: 48px; }
    .section-title {
      font-family: 'Fraunces', serif; font-weight: 500;
      font-size: clamp(24px, 4vw, 32px); color: var(--text);
      text-align: center; margin-bottom: 36px; line-height: 1.15;
    }
    .steps { display: flex; flex-direction: column; gap: 14px; }
    .step {
      display: flex; gap: 18px; align-items: flex-start;
      background: var(--white); border: 1px solid var(--border);
      border-radius: 18px; padding: 24px;
      box-shadow: 0 2px 12px rgba(0,0,0,.03);
    }
    .step-num {
      width: 36px; height: 36px; border-radius: 50%;
      background: var(--sage-light); color: var(--green);
      display: flex; align-items: center; justify-content: center;
      font-family: 'Fraunces', serif; font-weight: 500; font-size: 16px;
      flex-shrink: 0;
    }
    .step-content { flex: 1; }
    .step-title {
      font-size: 15px; font-weight: 700; color: var(--text);
      margin-bottom: 3px;
    }
    .step-text {
      font-size: 14px; color: var(--sage); line-height: 1.6;
    }

    /* ── MISSION CARD ── */
    .mission-section { padding-top: 0; padding-bottom: 48px; }
    .mission-card {
      background: var(--green-dark); border-radius: 24px;
      padding: clamp(36px, 5vw, 56px); text-align: center;
      color: white;
    }
    .mission-card h2 {
      font-family: 'Fraunces', serif; font-weight: 500;
      font-size: clamp(24px, 4vw, 32px); line-height: 1.2;
      margin-bottom: 16px; color: white;
    }
    .mission-card p {
      font-size: clamp(14px, 2vw, 16px); line-height: 1.7;
      color: rgba(255,255,255,.7); max-width: 480px;
      margin: 0 auto;
    }

    /* ── FINAL CTA ── */
    .final-cta-section {
      padding-top: 48px; padding-bottom: 80px; text-align: center;
    }
    .final-cta-section h2 {
      font-family: 'Fraunces', serif; font-weight: 500;
      font-size: clamp(28px, 5vw, 40px); color: var(--text);
      margin-bottom: 12px; line-height: 1.1;
    }
    .final-cta-section p {
      font-size: 15px; color: var(--sage); line-height: 1.6;
      max-width: 400px; margin: 0 auto 28px;
    }
    .final-cta-btn {
      display: inline-block; background: var(--green); color: white;
      padding: 14px 36px; border-radius: 100px; font-size: 15px; font-weight: 600;
      text-decoration: none; box-shadow: 0 8px 32px rgba(45,94,58,.25);
      transition: all .25s;
    }
    .final-cta-btn:hover { background: var(--green-dark); transform: translateY(-2px); box-shadow: 0 12px 40px rgba(45,94,58,.35); }

    /* ── FOOTER ── */
    footer {
      background: var(--text); padding: 40px;
      display: flex; align-items: center; justify-content: space-between;
      flex-wrap: wrap; gap: 16px;
    }
    .footer-brand { font-family: 'Fraunces', serif; font-size: 20px; color: white; font-weight: 400; }
    .footer-links { display: flex; gap: 24px; }
    .footer-links a { color: rgba(255,255,255,.45); text-decoration: none; font-size: 14px; transition: color .2s; }
    .footer-links a:hover { color: white; }
    .footer-copy { color: rgba(255,255,255,.3); font-size: 13px; width: 100%; }

    /* ── ANIMATIONS ── */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .reveal { opacity: 0; transform: translateY(20px); transition: opacity .5s ease, transform .5s ease; }
    .reveal.visible { opacity: 1; transform: translateY(0); }

    /* ── MOBILE ── */
    @media (max-width: 640px) {
      nav { padding: 16px 20px; }
      .nav-link { display: none; }
      .hero-bottom { flex-direction: column; align-items: center; gap: 12px; }
      .step { flex-direction: column; gap: 10px; }
      footer { flex-direction: column; align-items: flex-start; padding: 32px 24px; }
      .milestone-label { font-size: 9px; }
      .progress-milestones { gap: 2px; }
    }
  </style>
</head>
<body>

<nav>
  <a class="nav-logo" href="/">
    <div class="nav-mark"><img src="/logo-white.png" alt="Matchaa" /></div>
    <span class="nav-name">matchaa</span>
  </a>
  <div class="nav-links">
    <a class="nav-link" href="/waitlist">Waitlist</a>
    <a class="nav-cta" href="/waitlist">Join the mission</a>
  </div>
</nav>

<!-- HERO + PROGRESS -->
<section class="hero">
  <div class="container">
    <p class="hero-eyebrow">A public mission by matchaa</p>
    <h1>Road to 10,000<br>Friendships</h1>
    <p class="hero-sub">We're trying to help 10,000 people make one real friend. Every conversation, meetup, and friendship moves the number forward.</p>

    <!-- PROGRESS CARD -->
    <div class="progress-card">
      <div class="progress-card-title">Road to 10,000 Friendships</div>
      <div class="progress-numbers">
        <span class="progress-current" id="currentNum">0</span>
        <span class="progress-unit">friendships created</span>
      </div>
      <div class="progress-meta">
        <span class="progress-meta-item">10,000 goal</span>
        <span class="progress-meta-item" id="pctText">0% complete</span>
      </div>
      <div class="progress-track">
        <div class="progress-fill" id="progressFill" style="width: 0%;"></div>
      </div>
      <div class="progress-milestones">
        <div class="milestone" data-val="100">
          <span class="milestone-val">100</span>
          <span class="milestone-label">First community</span>
        </div>
        <div class="milestone" data-val="500">
          <span class="milestone-val">500</span>
          <span class="milestone-label">Momentum</span>
        </div>
        <div class="milestone" data-val="1000">
          <span class="milestone-val">1,000</span>
          <span class="milestone-label">Melbourne proven</span>
        </div>
        <div class="milestone" data-val="5000">
          <span class="milestone-val">5,000</span>
          <span class="milestone-label">Australia wide</span>
        </div>
        <div class="milestone" data-val="10000">
          <span class="milestone-val">10,000</span>
          <span class="milestone-label">Mission complete</span>
        </div>
      </div>
      <p class="progress-footnote">The first friendship starts at 1.</p>
    </div>

    <!-- JOINED + CTA -->
    <div class="hero-bottom">
      <div class="joined-stat">
        <span class="joined-num" id="joinedNum">0</span>
        <span class="joined-label">people joined the mission</span>
      </div>
      <a class="hero-cta" href="/waitlist">Join the mission</a>
    </div>
    <p class="hero-honest">Numbers update as real friendships are confirmed.</p>
  </div>
</section>

<!-- MISSION PULL-UP -->
<section class="mission-pullup">
  <div class="container reveal">
    <h2>This is bigger than downloads.</h2>
    <p>We don't want another app people scroll through. We want proof that strangers can still become real friends.</p>
  </div>
</section>

<div class="divider"></div>

<!-- HOW IT WORKS -->
<section class="how-section">
  <div class="container">
    <h2 class="section-title reveal">How we count a friendship</h2>
    <div class="steps">
      <div class="step reveal">
        <div class="step-num">1</div>
        <div class="step-content">
          <div class="step-title">Join Matchaa</div>
          <div class="step-text">Create your profile and find people who want the same kind of friendship.</div>
        </div>
      </div>
      <div class="step reveal">
        <div class="step-num">2</div>
        <div class="step-content">
          <div class="step-title">Start a real conversation</div>
          <div class="step-text">Message, plan something simple, and skip the awkward guessing.</div>
        </div>
      </div>
      <div class="step reveal">
        <div class="step-num">3</div>
        <div class="step-content">
          <div class="step-title">Meet in real life</div>
          <div class="step-text">When a meetup happens, the friendship counter moves forward.</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- MISSION CARD -->
<section class="mission-section">
  <div class="container">
    <div class="mission-card reveal">
      <h2>Matchaa is the tool.<br>The mission is 10,000 friendships.</h2>
      <p>We're starting from zero. No fake numbers. No inflated metrics. Just real people meeting real people, one friendship at a time.</p>
    </div>
  </div>
</section>

<!-- FINAL CTA -->
<section class="final-cta-section">
  <div class="container reveal">
    <h2>Be one of the first.</h2>
    <p>Join the waitlist and help create the first 10,000 Matchaa friendships.</p>
    <a class="final-cta-btn" href="/waitlist">Join the waitlist</a>
  </div>
</section>

<footer>
  <div class="footer-brand">matchaa</div>
  <div class="footer-links">
    <a href="/waitlist">Waitlist</a>
    <a href="/privacy">Privacy Policy</a>
    <a href="mailto:hello@matchaa.app">Contact</a>
  </div>
  <div class="footer-copy">&copy; 2026 Matchaa. All rights reserved.</div>
</footer>

<script>
  // ── DATA (swap for Supabase later) ──
  var DATA = {
    friendshipsCreated: 0,
    meetupsCompleted: 0,
    peopleJoinedMission: 0,
    goal: 10000
  };

  function renderStats() {
    var pct = Math.min((DATA.friendshipsCreated / DATA.goal) * 100, 100);

    document.getElementById('currentNum').textContent = DATA.friendshipsCreated.toLocaleString();
    document.getElementById('pctText').textContent = (pct < 1 && DATA.friendshipsCreated > 0 ? '<1' : Math.floor(pct)) + '% complete';
    document.getElementById('progressFill').style.width = Math.max(pct, DATA.friendshipsCreated > 0 ? 0.5 : 0) + '%';
    document.getElementById('joinedNum').textContent = DATA.peopleJoinedMission.toLocaleString();

    document.querySelectorAll('.milestone').forEach(function(el) {
      var val = parseInt(el.getAttribute('data-val'));
      if (DATA.friendshipsCreated >= val) {
        el.classList.add('reached');
      }
    });
  }

  renderStats();

  // ── SCROLL REVEAL ──
  var reveals = document.querySelectorAll('.reveal');
  var observer = new IntersectionObserver(function(entries) {
    entries.forEach(function(entry) {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.15 });
  reveals.forEach(function(el) { observer.observe(el); });
</script>

</body>
</html>
